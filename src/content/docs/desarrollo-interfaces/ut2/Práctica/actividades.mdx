---
title: Actividades Propuestas
description: Ejercicios y casos de estudio propuestos en la UT1 sobre Diseño de Interfaces y Usabilidad.
---

Esta sección centraliza todos los ejercicios prácticos y actividades propuestos a lo largo de la Unidad de Trabajo 1. Utiliza estos enlaces para acceder directamente a cada tarea y poner a prueba tus conocimientos.

---
## 🔹 Tareas Básicas

1. **Seleccionar elementos**  
   - Crea un HTML con:  
     - Un título `<h1>` con el texto: `Biblioteca Online`.  
     - Un párrafo `<p>` con el texto: `Explora los libros disponibles en nuestra colección.`  
     - Una lista `<ul>` con tres elementos `<li>`: `El Quijote`, `Cien Años de Soledad`, `La Odisea`.  
   - Desde JavaScript:  
     - Selecciona el `<h1>` usando `getElementById`.  
     - Selecciona todos los `<li>` usando `querySelectorAll`.  

2. **Modificar contenido**  
   - Cambia el texto del `<h1>` a: `Catálogo de la Biblioteca`.  
   - Cambia el contenido del párrafo a: `Encuentra clásicos y novedades en nuestra colección.`  
   - Añade estilos en línea al párrafo: `color: green` y `fontWeight: bold`.  

---

## 🔹 Tareas de Creación

3. **Añadir un libro a la lista**  
   - Con `createElement`, genera un `<li>` con el texto: `Don Juan Tenorio`.  
   - Añádelo al final de la lista de libros con `appendChild`.  

4. **Insertar un libro destacado al inicio**  
   - Crea un `<li>` con el texto: `Libro Destacado: La Divina Comedia`.  
   - Colócalo como primer elemento de la lista usando `insertBefore`.  

---

## 🔹 Tareas de Modificación Avanzada

5. **Atributos y clases**  
   - Añade un `<input>` de tipo texto con `id="buscador"`.  
   - Usa `setAttribute` para añadir un `placeholder` con el texto: `Buscar libro...`.  
   - Añade al `<h1>` la clase `titulo-principal` usando `classList.add()`.  
   - Alterna la clase `resaltado` en el párrafo con `classList.toggle()`.  

6. **innerHTML con precaución**  
   - Selecciona un `<div>` vacío con `id="info"`.  
   - Rellénalo con `innerHTML` para que contenga:  
     ```html
     <h2>Información de la Biblioteca</h2>
     <p>Horario: Lunes a Viernes de 9:00 a 20:00</p>
     ```  
   - Reflexiona: ¿qué pasaría si el contenido del párrafo viniera de un usuario desconocido?  

---

## 🔹 Tareas con Eventos

7. **Contador de visitas**  
   - Crea un botón `<button>` con el texto: `Registrar visita`.  
   - Crea un párrafo vacío `<p>` con `id="visitas"`.  
   - Cada vez que el botón sea presionado, incrementa un contador y actualiza el párrafo con el texto:  
     `Visitas registradas: X`.  

8. **Marcar libros leídos**  
   - Haz que al hacer clic en un `<li>` de la lista de libros se le añada o quite la clase `leido`.  
   - Si el `<li>` tiene la clase `leido`, su texto debería aparecer tachado gracias a CSS.  

---

## 🔹 Tareas de Rendimiento

9. **Añadir muchos libros de golpe**  
   - Usa un `DocumentFragment` para generar 50 elementos `<li>` con los textos:  
     `Libro 1`, `Libro 2`, … `Libro 50`.  
   - Añádelos todos de golpe al final de la lista de libros.  
   - Compara con hacerlo uno por uno.  

---

## 🔹 Proyecto Integrador

10. **Gestor de Biblioteca**  
    - Crea un formulario con:  
      - Un `<input>` con `id="titulo"` y `placeholder="Título del libro"`.  
      - Un botón con el texto: `Añadir Libro`.  
    - Cada vez que se envíe el formulario:  
      - Añade un nuevo `<li>` con el título escrito.  
      - Ese `<li>` debe tener también un botón `Eliminar` que borre el libro de la lista al hacer clic.  

---