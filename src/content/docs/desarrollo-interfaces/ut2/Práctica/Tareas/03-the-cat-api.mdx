---
title: '03: Explorando el Mundo Felino con TheCatAPI y tus Favoritos'
description: 'Desarrolla una aplicación web interactiva para descubrir gatos, gestionar tus favoritos y persistir tus elecciones con localStorage.'
---

¡Hola, amantes de los gatos! 🐾

Prepárense para sumergirse en el adorable mundo felino con esta práctica. Vamos a construir una aplicación web interactiva que no solo nos permitirá descubrir una infinidad de gatos, sino también guardar a nuestros favoritos para siempre (¡o al menos hasta que decidamos borrarlos!).

Utilizaremos la encantadora **TheCatAPI** para traer a estos peludos amigos a nuestra pantalla y la magia de `localStorage` para recordar nuestras elecciones.

---

## 🎯 **Tu Misión: La Galería Felina Personalizada**

Tu objetivo principal es desarrollar una **aplicación web completa** que te permita:

1.  **Explorar imágenes aleatorias de gatos** con un solo clic.
2.  **Marcar tus gatos preferidos como favoritos** y verlos en una galería especial.
3.  **Mantener tus favoritos guardados** incluso si cierras el navegador o recargas la página.
4.  **Gestionar tu colección de favoritos**, eliminando aquellos que ya no te roben el corazón.

---

## 🧠 **Habilidades que Desbloquearás**

Al completar esta práctica, habrás fortalecido tus conocimientos en:

*   **Comunicación con APIs REST:** Realizar peticiones `GET` a servicios externos para obtener datos.
*   **Asincronía en JavaScript:** Dominar `fetch` y `async/await` para manejar la carga de imágenes de forma eficiente.
*   **Manipulación del DOM:** Crear, actualizar y eliminar elementos HTML dinámicamente con JavaScript.
*   **Persistencia de Datos:** Utilizar `localStorage` para guardar información en el navegador del usuario.
*   **Diseño Interactivo:** Crear una interfaz de usuario atractiva y funcional para interactuar con la aplicación.

---

## 🧩 **El Desafío: Requisitos Clave**

Tu aplicación debe cumplir con las siguientes funcionalidades:

1.  **Visualización de Gatos:**
    *   Al cargar la página, debe mostrar una **imagen aleatoria de un gato** obtenida desde [TheCatAPI](https://thecatapi.com/).
    *   Un **botón "Cargar Nuevo Gato"** que, al hacer clic, muestre una nueva imagen aleatoria.

2.  **Gestión de Favoritos:**
    *   Un **botón "Añadir a Favoritos"** que guarde la imagen del gato actual en una lista.
    *   Una sección dedicada a mostrar la **"Galería de Favoritos"** debajo de la imagen principal.
    *   Cada gato en la galería de favoritos debe tener un **botón "Eliminar"** individual para quitarlo de la lista.
    *   Un **botón "Eliminar Todos los Favoritos"** para limpiar completamente la galería.

3.  **Persistencia de Datos:**
    *   Los gatos añadidos a favoritos deben **permanecer guardados** incluso si el usuario recarga la página o cierra el navegador, gracias a `localStorage`.

---

## 🎨 **Tu Toque Artístico: Requisitos de Diseño**

Queremos que tu Galería Felina no solo sea funcional, sino también visualmente atractiva. Aplica estos principios:

*   **Estilo Coherente:** Utiliza un diseño limpio y agradable. Puedes inspirarte en las otras prácticas y usar **Tailwind CSS** si lo deseas, o simplemente CSS puro.
*   **Organización Visual:** Usa **Flexbox o Grid** para organizar el contenido de la página de manera ordenada y responsiva.
*   **Feedback Visual:** Asegúrate de que los botones y las interacciones sean claras para el usuario.

---

## 💡 **Tu Caja de Herramientas: Pistas y Pasos Sugeridos**

Aquí tienes una guía para que no te pierdas en el camino y aproveches al máximo las herramientas disponibles:

### 🐱 **Interactuando con TheCatAPI**

TheCatAPI es una API gratuita y no requiere clave para esta práctica.

*   **Endpoint para imágenes aleatorias:**
    ```
    https://api.thecatapi.com/v1/images/search
    ```
    Esta URL te devolverá un array con objetos JSON, donde cada objeto contiene el `id` y la `url` de una imagen de gato. ¡Solo necesitas el primero del array!

    :::tip[¡Recuerda `fetch` y `async/await`!]
    Para obtener la imagen, usarás `fetch` dentro de una función `async`. No olvides `await` la respuesta y luego `await` la conversión a JSON (`response.json()`).
    :::

### 💾 **Guardando tus Favoritos con `localStorage`**

`localStorage` es tu aliado para la persistencia de datos en el navegador. Recuerda que solo puede guardar cadenas de texto.

*   **Conceptos clave:**
    | Método                               | Descripción                                     |
    | :----------------------------------- | :---------------------------------------------- |
    | `localStorage.setItem(clave, valor)` | Guarda un dato. Ambos deben ser texto.          |
    | `localStorage.getItem(clave)`        | Recupera un dato. Devuelve `null` si no existe. |
    | `localStorage.removeItem(clave)`     | Elimina un dato específico.                     |
    | `localStorage.clear()`               | Borra todo el almacenamiento local del sitio.   |

*   **Guardar y recuperar arrays/objetos:**
    Como `localStorage` solo guarda texto, necesitarás `JSON.stringify()` para convertir tus arrays (por ejemplo, un array de IDs de gatos favoritos) a texto antes de guardarlos, y `JSON.parse()` para convertirlos de nuevo a un array cuando los recuperes.

    ```javascript
    // Guardar un array de IDs de gatos favoritos
    const favoritos = ['abc123', 'def456'];
    localStorage.setItem('gatosFavoritos', JSON.stringify(favoritos));

    // Recuperar el array
    const listaFavoritosGuardada = JSON.parse(localStorage.getItem('gatosFavoritos')) || [];
    console.log(listaFavoritosGuardada); // ['abc123', 'def456']
    ```
    :::note
    En esta práctica, te recomendamos guardar un array con los `id` de los gatos favoritos. Cada vez que el usuario añada o elimine un gato, deberás actualizar este array en `localStorage`.
    :::

### 🛠️ **Consejos Adicionales**

*   **Estructura del HTML:** Prepara un `div` para la imagen principal, otro para los botones de acción y un contenedor para la galería de favoritos.
*   **IDs Únicos:** Cada imagen de gato de la API tiene un `id` único. ¡Úsalo para identificar y gestionar tus favoritos!
*   **Manejo de Eventos:** Asocia funciones a los eventos `click` de tus botones.
*   **Actualización del DOM:** Cada vez que añadas o elimines un favorito, asegúrate de que la "Galería de Favoritos" se actualice visualmente.